#include "palette.h"

const sf::Color Palette::kColors[] = {sf::Color(255, 255, 255),
                                      sf::Color(192, 192, 192),
                                      sf::Color(96, 96, 96),
                                      sf::Color(0, 0, 0)};
